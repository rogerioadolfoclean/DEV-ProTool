const { Pool } = require("pg");
const pool = new Pool({ connectionString: process.env.DATABASE_URL });
(async()=>{await pool.query(`CREATE TABLE IF NOT EXISTS listes_appels (id SERIAL PRIMARY KEY, tenant_id INT NOT NULL REFERENCES tenants(id), nom VARCHAR(160) NOT NULL, description TEXT, statut VARCHAR(20) NOT NULL DEFAULT 'active' CHECK (statut IN ('active','archivee','brouillon')), total_contacts INT NOT NULL DEFAULT 0, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS liste_appels_contacts (id SERIAL PRIMARY KEY, liste_id INT NOT NULL REFERENCES listes_appels(id) ON DELETE CASCADE, contact_id INT NOT NULL REFERENCES contacts(id) ON DELETE CASCADE, statut VARCHAR(20) NOT NULL DEFAULT 'a_appeler' CHECK (statut IN ('a_appeler','appele','repondu','sans_reponse','injoignable')), UNIQUE(liste_id,contact_id)); CREATE INDEX IF NOT EXISTS idx_listes_appels_tenant ON listes_appels(tenant_id,created_at DESC); CREATE INDEX IF NOT EXISTS idx_liste_contacts_liste ON liste_appels_contacts(liste_id);`); console.log('Migration listes appels OK');})().catch(e=>{console.error(e);process.exitCode=1}).finally(()=>pool.end());
