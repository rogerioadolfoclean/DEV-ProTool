const {Pool}=require('pg');const pool=new Pool({connectionString:process.env.DATABASE_URL});(async()=>{await pool.query(`CREATE TABLE IF NOT EXISTS rendez_vous (id SERIAL PRIMARY KEY, tenant_id INT NOT NULL REFERENCES tenants(id), client_id INT, titre VARCHAR(180) NOT NULL, description TEXT, debut TIMESTAMPTZ NOT NULL, fin TIMESTAMPTZ NOT NULL, statut VARCHAR(20) NOT NULL DEFAULT 'planifie' CHECK(statut IN ('planifie','confirme','termine','annule')), lieu VARCHAR(220), notes TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS idx_rdv_tenant_debut ON rendez_vous(tenant_id,debut);`);console.log('Migration rendez-vous OK')})().catch(e=>{console.error(e);process.exitCode=1}).finally(()=>pool.end());