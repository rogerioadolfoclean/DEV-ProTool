const {Pool}=require('pg');const pool=new Pool({connectionString:process.env.DATABASE_URL});(async()=>{await pool.query(`CREATE TABLE IF NOT EXISTS operator_messages (id BIGSERIAL PRIMARY KEY,tenant_id INT NOT NULL REFERENCES tenants(id),channel VARCHAR(20) NOT NULL CHECK(channel IN('sms','voice','whatsapp')),to_address VARCHAR(180) NOT NULL,body TEXT,provider VARCHAR(80),provider_message_id VARCHAR(180),status VARCHAR(30) NOT NULL DEFAULT 'queued',status_reason TEXT,queued_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),sent_at TIMESTAMPTZ,delivered_at TIMESTAMPTZ,failed_at TIMESTAMPTZ,updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS idx_operator_messages_tenant_status ON operator_messages(tenant_id,status,queued_at DESC);CREATE UNIQUE INDEX IF NOT EXISTS uq_operator_provider_message ON operator_messages(provider,provider_message_id) WHERE provider_message_id IS NOT NULL;`);console.log('Migration operator delivery OK')})().catch(e=>{console.error(e);process.exitCode=1}).finally(()=>pool.end());