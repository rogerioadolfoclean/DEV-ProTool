"use client";
import { useEffect,useState } from "react";
type Contact={id:number;name:string;phone:string;email?:string;status?:string};
export default function ContactsPage(){const [items,setItems]=useState<Contact[]>([]),[q,setQ]=useState("");const load=()=>fetch(`/api/contacts?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(x=>setItems(x.data||[]));useEffect(()=>{load()},[]);return <section className="space-y-5"><div><h1 className="text-3xl font-bold">♧ Contacts</h1><p className="text-slate-400">Recherche et segmentation des contacts commerciaux.</p></div><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Rechercher nom, téléphone, email…" className="flex-1 rounded-lg bg-slate-950 border border-slate-700 p-3"/><button onClick={load} className="rounded-lg bg-sky-600 px-4">Rechercher</button></div><div className="rounded-xl border border-slate-700 overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b border-slate-700 text-slate-400"><th className="p-3">Nom</th><th className="p-3">Téléphone</th><th className="p-3">Email</th><th className="p-3">Statut</th><th className="p-3">IA</th></tr></thead><tbody>{items.map(c=><tr key={c.id} className="border-b border-slate-800"><td className="p-3">{c.name}</td><td className="p-3">{c.phone}</td><td className="p-3">{c.email||'—'}</td><td className="p-3">{c.status||'—'}</td><td className="p-3"><a href={`/console/ai-vendeur?contact=${c.id}`} className="rounded bg-emerald-700 px-3 py-1">Vendre</a></td></tr>)}</tbody></table></div></section>}
